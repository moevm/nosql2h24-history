<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
</head>
<body>
<h1><%= title %></h1>

<% if (streets.length === 0) { %>
    <p>Ничего не найдено по заданным критериям.</p>
<% } else { %>
    <ul>
        <% streets.forEach(function(street) { %>
            <li><%= street.name %> (<%= street.type %>, <%= street.district %>)</li>
        <% }); %>
    </ul>
<% } %>

<a href="/main/streets">Назад к фильтрам</a>
</body>
</html>