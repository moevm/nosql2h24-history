<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Дома на улице <%= streetKey %></title>
  <link rel="stylesheet" href="/styles.css">
  <!-- Инициализация PostHog (если используется) -->
</head>
<body>
  <header>
    <h1>Дома на улице <%= streetKey %></h1>
    <nav>
      <a href="/">Главная</a> |
      <a href="/streets">Список улиц</a>
    </nav>
  </header>
  <main>
    <p>Найдено домов: <%= houses.length %></p>
    <table>
      <thead>
        <tr>
          <th>Адрес</th>
          <th>Год</th>
          <th>Этажей</th>
          <th>Квартиры</th>
          <th>Состояние</th>
          <th>УК</th>
          <th>Серия, тип</th>
        </tr>
      </thead>
      <tbody>
        <% if (houses.length > 0) { %>
          <% houses.forEach(function(house) { %>
            <tr>
              <td><%= house.address %></td>
              <td><%= house.year %></td>
              <td><%= house.floors %></td>
              <td><%= house.apartments %></td>
              <td><%= house.condition %></td>
              <td><%= house.management_company %></td>
              <td><%= house.series_type %></td>
            </tr>
          <% }); %>
        <% } else { %>
          <tr>
            <td colspan="7">Дома не найдены.</td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </main>
  <footer>
    <p>&copy; 2023 City Data App</p>
  </footer>
</body>
</html>
