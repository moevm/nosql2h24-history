<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Дома на улице <%= streetKey %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
<header>
    <h1>Дома на улице <%= streetKey %></h1>
    <nav>
        <a href="/">Главная</a> |
        <a href="/streets">Список улиц</a>
    </nav>
</header>
<main>
    <p>Найдено домов: <%= houses.length %></p>
    <table>
        <thead>
        <tr>
            <th>Фото</th>
            <th>Адрес</th>
            <th>Год постройки</th>
            <th>Этажей</th>
            <th>Квартиры</th>
            <th>Состояние</th>
            <th>УК</th>
            <th>Серия</th>
            <th>Район</th>
        </tr>
        </thead>
        <tbody>
        <% if (houses.length > 0) { %>
            <% houses.forEach(function(house) { %>
                <tr>
                    <td><img src="<%= house.photo %>" alt="Фото дома" width="100"></td>
                    <td><%= house.street %>, <%= house.house_number %></td>
                    <td><%= house.construction_year %></td>
                    <td><%= house.floors %></td>
                    <td><%= house.apartments %></td>
                    <td><%= house.condition %></td>
                    <td><%= house.management_company %></td>
                    <td><%= house.series %></td>
                    <td><%= house.district %></td>
                </tr>
            <% }); %>
        <% } else { %>
            <tr>
                <td colspan="9">Дома не найдены.</td>
            </tr>
        <% } %>
        </tbody>
    </table>
</main>
<footer>
    <p>&copy; 2023 City Data App</p>
</footer>
</body>
</html>
